service : {
  cyclesBalance: () -> (nat) query;
  delete: () -> (principal);
  init: () -> (principal);
  installCode: (blob, blob) -> (principal);
  storageLoadWasm: (vec nat8) -> (record {
                                    chunks: nat;
                                    total: nat;
                                  });
  storateResetWasm: () -> ();
  wasmHash: () -> (blob);
}
