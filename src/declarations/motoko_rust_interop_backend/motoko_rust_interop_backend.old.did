service : {
  delete: () -> (principal);
  init: () -> (principal);
  installCode: (principal, blob, blob) -> ();
  storageLoadWasm: (vec nat8) -> (record {
                                    chunks: nat;
                                    total: nat;
                                  });
  storateResetWasm: () -> ();
  wasmHash: () -> (blob);
}
